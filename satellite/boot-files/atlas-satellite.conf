# Atlas Satellite Configuration (OPTIONAL)
# The satellite auto-discovers the server via mDNS.
# Only edit if you need to override defaults:
# ATLAS_SERVER_URL=ws://192.168.1.100:5100/ws/satellite
# ATLAS_HOSTNAME=atlas-sat-kitchen
